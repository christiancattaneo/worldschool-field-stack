# Secrets — never commit keys. Copy .env.example to .env.local and fill it in.
.env
.env.local
.env.*.local
*.key
*.pem

# Python
__pycache__/
*.pyc
.venv/
venv/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Node (if a build grows a JS side)
node_modules/
